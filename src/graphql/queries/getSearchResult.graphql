query GetSearchResults($search: String!) {
    getSearchResults(options: { search: $search }) {
        profiles {
            id
            name
            username
            img
        }
        error
    }
}
